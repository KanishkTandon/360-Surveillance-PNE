# ──────────────────────────────────────────────
#  360° Surveillance System — Python Dependencies
#  Edge-Optimised (CPU-Only, INT8)
# ──────────────────────────────────────────────

# ── AI / CV ──────────────────────────────────
ultralytics>=8.1.0          # YOLOv8  (export + fallback inference)
openvino>=2024.0.0          # OpenVINO runtime + AsyncInferQueue
nncf>=2.7.0                 # Neural Network Compression (INT8 PTQ)
opencv-python>=4.9.0        # cv2

# ── Backend API ──────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# ── Frontend (lightweight — Streamlit optional) ──
# streamlit>=1.31.0         # Replaced by index.html (vanilla JS)
requests>=2.31.0            # Only needed if using old Streamlit dashboard
# pandas>=2.2.0             # Only needed if using old Streamlit dashboard

# ── Utilities ────────────────────────────────
numpy>=1.26.0
